# تحديثات صفحة إنشاء حساب الكورسات

## التحديثات المضافة

### 1. عرض السعر تلقائياً بجانب كل كورس ✅
- يتم عرض سعر الكورس بجانب اسمه في قائمة الكورسات
- إذا كان هناك سعر قديم (خصم)، يتم عرضه مشطوباً
- التصميم يدعم عرض الوصف القصير للكورس

### 2. قسم معلومات الدفع 💳
يظهر هذا القسم تلقائياً عند اختيار أي كورس ويتضمن:

#### أ. أرقام التحويل
- **01098453187**
- **01210178529**
- زر نسخ سريع لكل رقم

#### ب. اختيار طريقة التحويل
خيارين متاحين:
- محفظة
- حساب بنكي / انستا باي

#### ج. رفع صورة إثبات التحويل
- منطقة سحب وإفلات للصورة
- معاينة الصورة قبل الرفع
- يقبل جميع صيغ الصور

### 3. حفظ البيانات في Appwrite

عند التسجيل، يتم حفظ:
- `paymentMethod`: طريقة التحويل المختارة
- `paymentProofId`: معرف صورة إثبات التحويل في Appwrite Storage
- `courses`: قائمة الكورسات المختارة
- `paymentStatus`: حالة الدفع (pending أو free)

### 4. التحقق من البيانات

- لا يمكن إتمام التسجيل بدون اختيار طريقة التحويل
- لا يمكن إتمام التسجيل بدون رفع صورة إثبات التحويل
- إذا لم يتم اختيار أي كورس، لا يظهر قسم الدفع

## الملفات المعدلة

- `portal/login-courses.html`: تم تحديث واجهة المستخدم والمنطق البرمجي

## ملاحظات مهمة

1. **Bucket التخزين**: حالياً يتم استخدام نفس bucket الخاص بصور الكورسات (`Bousla-Courses`). يمكن إنشاء bucket منفصل لصور إثبات الدفع.

2. **الصلاحيات**: تأكد من أن صلاحيات الـ bucket تسمح بالرفع من المستخدمين.

3. **عرض الصور في لوحة التحكم**: يمكن للأدمن عرض صورة إثبات التحويل من خلال:
```javascript
const imageUrl = storage.getFileView(BUCKETS.COURSE_THUMBNAILS, subscriber.paymentProofId);
```

## التحسينات المستقبلية المقترحة

1. إنشاء bucket منفصل لصور إثبات الدفع
2. إضافة validation لحجم الصورة (مثلاً: أقل من 5MB)
3. إضافة ضغط تلقائي للصور قبل الرفع
4. إضافة إشعار للأدمن عند تسجيل مشترك جديد مع دفع

---

## عرض معلومات الدفع في لوحة التحكم (Admin Dashboard)

### الميزات المضافة للأدمن:

#### 1. **قسم معلومات الدفع في نافذة تعديل المشترك** 💳

عند فتح تفاصيل أي مشترك من لوحة التحكم، يظهر قسم "معلومات الدفع" إذا كان المشترك قد قام برفع معلومات الدفع:

**يتضمن القسم:**
- **طريقة التحويل**: عرض الطريقة المختارة (محفظة أو حساب بنكي/انستا باي)
- **صورة إثبات التحويل**: 
  - عرض الصورة المرفوعة
  - إمكانية الضغط على الصورة لعرضها بالحجم الكامل
  - تأثير hover يوضح أن الصورة قابلة للنقر

#### 2. **التحقق من الدفع**

- يمكن للأدمن مراجعة صورة إثبات التحويل
- بعد التحقق، يمكن تغيير حالة الدفع من "بانتظار الدفع" إلى "تم الدفع"
- ملاحظة توضيحية تذكر الأدمن بإمكانية تغيير حالة الدفع بعد التحقق

#### 3. **التخزين**

- يتم استخدام نفس Bucket الموجود (`Bousla-Courses`) لتخزين صور إثبات الدفع
- يتم حفظ معرف الصورة في حقل `paymentProofId` في قاعدة البيانات
- عرض الصورة باستخدام:
```javascript
const imageUrl = storage.getFileView(BUCKETS.COURSE_THUMBNAILS, subscriber.paymentProofId);
```

### الملفات المعدلة:

1. **`portal/login-courses.html`**: 
   - إضافة قسم معلومات الدفع في نموذج التسجيل
   - رفع صورة إثبات التحويل إلى Appwrite Storage
   - حفظ طريقة التحويل ومعرف الصورة

2. **`admin/dashboard.html`**:
   - إضافة قسم عرض معلومات الدفع في نافذة تعديل المشترك
   - دالة `viewPaymentProofFullscreen()` لعرض الصورة بالحجم الكامل
   - تحديث دالة `editSubscriber()` لتحميل وعرض معلومات الدفع

### كيفية الاستخدام:

1. **للمشترك**:
   - اختيار الكورسات المطلوبة
   - اختيار طريقة التحويل
   - رفع صورة إثبات التحويل
   - إرسال النموذج

2. **للأدمن**:
   - الذهاب إلى قسم "المشتركين" في لوحة التحكم
   - الضغط على أيقونة "عرض التفاصيل" (👁️) بجانب اسم المشترك
   - مراجعة قسم "معلومات الدفع"
   - الضغط على الصورة لعرضها بالحجم الكامل
   - التحقق من صحة التحويل
   - تغيير حالة الدفع إلى "تم الدفع" إذا كان التحويل صحيحاً
   - حفظ التغييرات

